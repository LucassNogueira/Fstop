import React from "react";
import Cards from "../Cards";
import Splash from "../Splash";

const home = () => {
  return (
    <main className="relative ">
      <Splash />

      <Cards />
    </main>
  );
};

export default home;
